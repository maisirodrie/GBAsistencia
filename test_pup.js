import puppeteer from 'puppeteer'; import {generarCartaoHTML} from './server/utils/cartaoTemplate.js'; (async () => { const html = generarCartaoHTML({}); const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); await page.setContent(html); await page.emulateMediaType('screen'); await page.evaluate(() => { const s = document.createElement('style'); s.textContent = '* { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; } @page { size: 19.4cm 13.4cm; margin: 0; } html, body { width: 100%; height: 100%; margin: 0; padding: 0; overflow: hidden; }'; document.head.appendChild(s); }); const pdf = await page.pdf({width: '19.4cm', height: '13.4cm', printBackground: true, margin: { top: 0, right: 0, bottom: 0, left: 0 }, pageRanges: '1'}); require('fs').writeFileSync('test.pdf', pdf); await browser.close(); console.log('Saved', pdf.length); })();

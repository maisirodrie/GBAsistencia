import fs from 'fs'; import puppeteer from 'puppeteer'; import { generarCartaoHTML } from './server/utils/cartaoTemplate.js'; (async () => { const html = generarCartaoHTML({nombre: 'Juan'}); const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); await page.setContent(html); await page.evaluate(() => { const s = document.createElement('style'); s.textContent = '* { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; } @page { size: A4 portrait; margin: 0; } html, body { width: 100% !important; height: 100% !important; margin: 0 !important; padding: 0 !important; display: flex !important; align-items: center !important; justify-content: center !important; background: white !important; }'; document.head.appendChild(s); }); const pdf = await page.pdf({ format: 'A4', printBackground: true, margin: { top: 0, right: 0, bottom: 0, left: 0 } }); fs.writeFileSync('test_A4.pdf', Buffer.from(pdf)); await browser.close(); console.log('Saved', pdf.length); })();
